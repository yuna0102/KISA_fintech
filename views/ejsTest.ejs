<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
    <h1>안녕 클레오 파트라!</h1>
    <p>세상에서 제일 가는 포테이토칩</p>
    <!-- 요소가 많으면 어떤 건지 모르기 때문에 id를 부여해서 특정지어줌 -->
    <input type="text" id="inputText">
    <input type="text" id="inputText2">
    <button id="firstButton">버튼</button>
    <script src="https://code.jquery.com/jquery-3.5.1.js" integrity="sha256-QWo7LDvxbWT2tbbQ97B53yJnYU3WhH/C8ycbRAkjPDc=" crossorigin="anonymous"></script>
    <script>
        //front 작업
        //$는 jquery 라이브러리 쓰겠다고 선언
        //요소를 선택하는 선택자 selector를 통해 특정하기
        // '#' 아이디 불러오기
        $("#firstButton").click(function() { //click 이벤트 발생 -> 중괄호 안의 기능이 실행
            var userInput = $("#inputText").val(); //inputText라는 id를 가진 input의 value값을 가져와서 userInput이라는 변수에 넣음
            var userInput2 = $("#inputText2").val();
            //ajax를 써서 data를 전달하는 라이브러리
            //front-server를 연결시켜주는 작업 : req-res
            $.ajax({
                url: "/userData", //server와 front가 같은 파일 위치에 있음
                type: "POST", //http에 4가지 method : 
                data: {
                    //key-value 형식으로 object로 묶어줌
                    userData1 : userInput,
                    userData2 : userInput2
                },
                success: function (data) { //data안에 server에서 보낸 response가 담겨있음
                    console.log(data);
                    if(data === true) {
                        alert("요청성공"); //웹사이트 알림
                    }
                },
            });
        })
    </script>
</body>
</html>